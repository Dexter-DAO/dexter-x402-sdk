node_modules/
dist/
*.log
.DS_Store
.env
.env.local
coverage/

test-wallet-base.json

# Environment files with secrets
.env
.env.*
*.env

# Test wallet files
test-wallet*.json

# Model evaluation results (keep the directory structure)
test/model-eval/results/*
!test/model-eval/results/.gitkeep

# Proof-run receipts (mainnet tx evidence — never committed)
scripts/receipts/

# Internal agent-workflow docs (plans/specs/research) — private, not for the public SDK
docs/superpowers/

# Internal/working docs — private, not for the public SDK (kept locally)
docs/
